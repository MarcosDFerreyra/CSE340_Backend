<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <% allUsers.forEach(user => {%>
            <li>
                <p><%= user.name %></p>
                <p><%= user.email%></p>
                <p><%= user.role%></p> 
            </li>
            <% }) %>
    </main>
    <%- include('partials/footer') %>